import { Touchable } from '@/shared/ui/styled/Styled'
import styled from 'styled-components/native'

export const TouchableInputWrapper = styled(Touchable)`
  width: 100%;
  height: 100%;
  position: absolute;
  z-index: 5;
`
