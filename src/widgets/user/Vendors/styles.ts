import { StyleSheet } from 'react-native'
import styled from 'styled-components/native'

export const Separator = styled.View`
  width: 13px;
`

export const styles = StyleSheet.create({
  list: { paddingLeft: 44, paddingRight: 5 },
})
